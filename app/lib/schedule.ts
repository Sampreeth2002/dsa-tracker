// Backwards-compatible re-export.
// The schedule is now derived at runtime from the dataset in ./dataset.ts
// (single source of truth), so there is no static map to regenerate.
export { PROBLEM_SCHEDULE } from './dataset';
